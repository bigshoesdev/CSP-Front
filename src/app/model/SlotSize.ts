export namespace SlotSize_Ns {
    export enum SlotSize {
        slot5min = 5,
        slot10min = 10,
        slot15min = 15,
        slot30min = 30,
        slot60min = 60
    }

    export const $all: number[] = [
        SlotSize.slot5min,
        SlotSize.slot10min,
        SlotSize.slot15min,
        SlotSize.slot30min,
        SlotSize.slot60min
    ];
}
