export interface ClientSimpleMatchingInfoItem {
    time: string;
    service: string; // name of service
    room: string; // name of room
    therapist: string; // name of therpaist
    duration: number; // duration in minutes
}
