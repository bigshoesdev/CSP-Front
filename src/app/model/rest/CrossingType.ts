export namespace CrossingType_Ns {
    export enum CrossingType {
        service = <any>'service',
        event = <any>'event'
    }

    export const $all: any[] = [
        CrossingType.service,
        CrossingType.event
    ];
}
